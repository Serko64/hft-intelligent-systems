"""User-interface layer (pygame).

- app.py            — the main window + event loop (entry point)
- theme.py          — fonts and colour helpers
- camera.py         — shared drawing: track blitting, world→screen, rays, markers
- menu_view.py      — the circuit-selection menu
- training_view.py  — the live training view (all cars + stats)
- replay_view.py    — replay browser, replay playback, and "load & drive"
- hud.py            — speed/throttle HUD overlays
- storage.py        — load/save replays and best lap times
"""
